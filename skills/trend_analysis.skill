# 名称：trend_analysis
# 描述：对单个合约执行完整的多周期趋势分析，可单独调用也可被 market_scanner 调用
# 触发方式：手动触发，接收参数 symbol
# 入口：通过 ai_analysis.analyze_symbol() 调用

## 输入参数：
- symbol: string，例如 "BTC/USDT:USDT"
- mode: string，"text"（默认）或 "visual"，读自 settings.yaml analysis.mode

## 执行步骤：

### 第一步：获取多周期数据
- 调用 `fetch_kline.fetch_multi_timeframe(symbol, exchange)`
- 调用 `calculate_support_resistance(data["4h"])` 获取支撑阻力位
- 数据获取失败则返回错误并终止

### 第二步：生成图表（日志存档）
- 调用 `generate_chart.generate_multi_chart()` 生成四周期 PNG
- 两种模式下均生成，text 模式仅用于日志存档

### 第三步：调用统一分析入口
调用 `ai_analysis.analyze_symbol(multi_tf_data, symbol, support_levels, resistance_levels, image_paths)`

**text 模式内部流程：**
1. `indicator_engine.generate_market_snapshot()` 计算全部指标并生成结构化文本快照
   - EMA(20/50/200) 排列、ADX/+DI/-DI、RSI(14)、成交量/MA5 比值
   - 裸K形态识别（吞没线/锤子线/内包线/Pin Bar/十字星）
   - 各周期趋势结构（HH/HL/LH/LL）
2. `indicator_engine.rule_engine_filter()` 单边趋势预过滤门卫
   - 规则一：日线趋势必须非横盘（ADX >= adx_trending_threshold）
   - 规则二：15m/1h/4h 中至少 min_trending_timeframes 个周期与日线方向对齐
   - 规则三：至少一个周期量比 >= volume_ratio_threshold
   - 任一不通过：返回 signal=wait，_analysis_mode=rule_filter_rejected，节省 token
3. 规则通过后调用文本LLM：qwen3.5-plus → kimi-k2.5（兜底）
   - 发送结构化快照文本，要求输出标准 JSON 决策

**visual 模式内部流程：**
- 将四张图表路径传入视觉LLM降级链：qwen3-vl-flash → qwen3-vl-plus → qwen-vl-max

### 第四步：返回决策
标准决策 JSON 格式（两种模式输出一致）：
```json
{
  "signal": "long/short/wait/close",
  "confidence": "high/medium/low",
  "signal_strength": 1-10,
  "alignment_score": 0-4,
  "entry_price": 数字,
  "stop_loss": 数字,
  "take_profit": 数字,
  "risk_reward": "1:X.X",
  "volume_confirmed": true/false,
  "divergence_risk": true/false,
  "structure_broken": true/false,
  "reason": "不少于100字的分析",
  "_model_used": "模型名称",
  "_analysis_mode": "text_llm/visual/rule_filter_rejected"
}
```

## 错误处理：
- K线数据获取失败：返回 signal=wait，发送告警通知
- 规则引擎异常：降级为直接调用LLM
- LLM 全部模型失败：返回 signal=wait，reason 说明原因
